export const inputStyle = ({ theme }) => ({
    background: theme.inputColor,
    borderRadius: "3px",
    border: "transparent",
    padding: "10px",
    lineHeight: 1.5,
    outline: 0,
    width: '100%'
});
